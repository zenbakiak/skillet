# Skillet HTTP Server Docker Configuration

# Server Configuration
DOCKER_PORT=8080

# Authentication Tokens (IMPORTANT: Use strong, unique tokens in production!)
# Leave empty to disable authentication
AUTH_TOKEN=your-secret-auth-token-here
ADMIN_TOKEN=your-secret-admin-token-here

# Example secure tokens (generate your own):
# AUTH_TOKEN=sk_auth_$(openssl rand -hex 32)
# ADMIN_TOKEN=sk_admin_$(openssl rand -hex 32)

# Optional: Custom hooks directory
# If you want to mount hooks from a different location, uncomment and modify:
# HOOKS_DIR=/path/to/your/custom/hooks